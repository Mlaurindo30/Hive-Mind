[pytest]
markers =
    integration: requires live backends or real API keys (skipped in CI by default)
    real: knowledge-frente acceptance — real SQLite/Ollama/services, no mocks (gate de fase K*)
    requires_service(*services): skip automatically if a named real service is offline (ollama, milvus, falkordb, claude_mem, ragflow)
