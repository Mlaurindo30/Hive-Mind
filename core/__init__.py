"""Hive-Mind core package.

Wrap-around utilities for the brain: database, telemetry, auth, federation,
LLM clients, embeddings, semantic search, schema management.

Imports here are safe at package-load time (no circular deps).
"""