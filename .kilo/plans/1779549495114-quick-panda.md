# Plano de Correção e Melhoria — Sinapse Agent

> Objetivo: Corrigir todos os 24 gaps identificados e implementar suite de testes
> completa para garantir 100% de funcionalidade do sistema.
> Data: 2026-05-23

---

## Resumo Executivo

| Fase | Itens | Esforço estimado | Prioridade |
|------|-------|-----------------|------------|
| Fase 0 — Smoke tests (diagnóstico) | 5 testes | 1h | Imediato |
| Fase 1 — Correções críticas | 6 correções | 4h | ALTÍSSIMA |
| Fase 2 — Correções altas | 8 correções | 6h | ALTA |
| Fase 3 — Melhorias médias | 10 melhorias | 8h | MÉDIA |
| Fase 4 — Testes unitários | 41 testes | 6h | ALTA |
| Fase 5 — Testes integração + E2E | 42 testes | 8h | ALTA |
| Fase 6 — CI/CD + documentação | 5 itens | 4h | MÉDIA |
| **Total** | **88 testes + 24 correções** | **~37h** | |

---

## FASE 0 — Smoke Tests (Diagnóstico Imediato)

**Arquivo**: `tests/smoke/test_smoke.sh`

```bash
#!/bin/bash
# Smoke tests — rodar antes de qualquer deploy ou desenvolvimento
# Uso: bash tests/smoke/test_smoke.sh
# Deve passar em < 5 minutos

set -euo pipefail
PASS=0; FAIL=0

check() {
    local desc="$1"; shift
    if "$@" >/dev/null 2>&1; then
        echo "  ✓ $desc"; ((PASS++))
    else
        echo "  ✗ $desc"; ((FAIL++))
    fi
}

echo "=== Sinapse Agent — Smoke Tests ==="
echo ""

# S0.1 — Binários no PATH
echo "[S0.1] Binários:"
check "python3" command -v python3
check "graphify" command -v graphify
check "nmem" command -v nmem
check "rtk" command -v rtk
check "bun" command -v bun
check "node" command -v node
check "sqlite3" command -v sqlite3

# S0.2 — graph.json existe e é válido
echo "[S0.2] Knowledge Graph:"
GRAPH="cerebro/graphify-out/graph.json"
[ -f "$GRAPH" ] && echo "  ✓ graph.json exists ($(du -h "$GRAPH" | cut -f1))" && ((PASS++)) || { echo "  ✗ graph.json missing"; ((FAIL++)); }
python3 -c "
import json
g = json.load(open('$GRAPH'))
assert 'nodes' in g and len(g['nodes']) > 100, 'Too few nodes'
assert 'links' in g, 'No links key'
" 2>/dev/null && echo "  ✓ graph.json valid ($(python3 -c "import json;g=json.load(open('$GRAPH'));print(f\"{len(g['nodes'])} nodes, {len(g['links'])} edges\")"))" && ((PASS++)) || { echo "  ✗ graph.json invalid"; ((FAIL++)); }

# S0.3 — Claude-mem worker
echo "[S0.3] Claude-mem Worker:"
HEALTH=$(curl -s --max-time 3 http://127.0.0.1:37700/health 2>/dev/null || echo '{"status":"down"}')
echo "$HEALTH" | python3 -c "import json,sys; assert json.load(sys.stdin).get('status')=='ok'" 2>/dev/null && echo "  ✓ worker healthy" && ((PASS++)) || { echo "  ✗ worker offline or unhealthy"; ((FAIL++)); }

# S0.4 — NeuralMemory
echo "[S0.4] NeuralMemory:"
nmem recall "test" >/dev/null 2>&1 && echo "  ✓ nmem functional" && ((PASS++)) || { echo "  ✗ nmem error"; ((FAIL++)); }

# S0.5 — RTK
echo "[S0.5] RTK:"
rtk --version >/dev/null 2>&1 && echo "  ✓ rtk functional" && ((PASS++)) || { echo "  ✗ rtk error"; ((FAIL++)); }

# S0.6 — Plugin sinapse-memory presente
echo "[S0.6] Plugin:"
[ -f "plugins/hermes/sinapse-memory.py" ] && echo "  ✓ plugin source exists" && ((PASS++)) || { echo "  ✗ plugin source missing"; ((FAIL++)); }
[ -f "$HOME/.hermes/plugins/sinapse-memory/__init__.py" ] && echo "  ✓ plugin installed in Hermes" && ((PASS++)) || { echo "  ⊘ plugin not installed in Hermes (optional)"; ((PASS++)); }

# S0.7 — Systemd service
echo "[S0.7] Systemd:"
systemctl --user is-active sinapse-claude-mem.service >/dev/null 2>&1 && echo "  ✓ service active" && ((PASS++)) || { echo "  ⊘ service not active (optional)"; ((PASS++)); }

echo ""
echo "Results: $PASS passed, $FAIL failed"
[ "$FAIL" -eq 0 ] && echo "SMOKE: PASS" || { echo "SMOKE: FAIL"; exit 1; }
```

---

## FASE 1 — Correções Críticas (ALTÍSSIMA prioridade)

### 1.1 — FIX: Exception Swallowing no Motor de Busca
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.2.7 — `except Exception: continue` esconde bugs
**Solução**: Logar exceções em stderr, distinguir erros esperados (ConnectionRefused) de bugs (KeyError)

```python
import sys
import traceback

def _query_vault_knowledge(query: str) -> Optional[Dict[str, Any]]:
    if not query or not query.strip():
        return None

    for backend in _READ_BACKENDS:
        try:
            result = backend(query)
            if result:
                return result
        except (URLError, OSError, subprocess.TimeoutExpired, FileNotFoundError):
            # Expected: backend unreachable — silently skip
            continue
        except Exception as e:
            # Unexpected: bug in backend — log and skip
            print(f"[sinapse] Backend error ({backend.__name__}): {e}", file=sys.stderr)
            traceback.print_exc(file=sys.stderr)
            continue

    return None
```

### 1.2 — FIX: Race Condition Cron vs Plugin
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.1.5 — Leitura parcial do graph.json durante rebuild
**Solução**: Tentar ler com retry + fallback para None

```python
import time

def _backend_graphify(query: str) -> Optional[Dict[str, Any]]:
    if not os.path.isfile(GRAPH_JSON):
        return None

    # Retry loop: graph.json pode estar sendo escrito pelo cron
    for attempt in range(3):
        try:
            with open(GRAPH_JSON, "r") as f:
                graph = json.load(f)
            break
        except (json.JSONDecodeError, OSError):
            if attempt < 2:
                time.sleep(0.1)
            else:
                return None

    # ... resto da lógica existente ...
```

**Adicional**: Modificar `scripts/build-graph.sh` para usar arquivo temporário + rename atômico:

```bash
#!/bin/bash
PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
VAULT_DIR="$PROJECT_ROOT/cerebro"
GRAPH_OUT="$VAULT_DIR/graphify-out"
TMP_GRAPH="$GRAPH_OUT/.graph.json.tmp"

# Backup do graph atual
[ -f "$GRAPH_OUT/graph.json" ] && cp "$GRAPH_OUT/graph.json" "$GRAPH_OUT/graph.json.bak"

# Gerar em arquivo temporário e renomear atomicamente
graphify update "$VAULT_DIR" 2>&1

# Verificar se o novo graph.json é válido
if python3 -c "import json; json.load(open('$GRAPH_OUT/graph.json'))" 2>/dev/null; then
    echo "graph.json valid — build completo"
else
    echo "ERRO: graph.json inválido, restaurando backup" >&2
    [ -f "$GRAPH_OUT/graph.json.bak" ] && mv "$GRAPH_OUT/graph.json.bak" "$GRAPH_OUT/graph.json"
    exit 1
fi
```

### 1.3 — FIX: Atomicidade na Escrita
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.1.3 — Arquivos podem ser truncados em falha de escrita
**Solução**: Escrever em arquivo temporário + rename atômico

```python
import tempfile

def _atomic_write(filepath: str, content: str) -> bool:
    """Escreve arquivo atomicamente via temp + rename."""
    dirname = os.path.dirname(filepath)
    os.makedirs(dirname, exist_ok=True)
    try:
        fd, tmp_path = tempfile.mkstemp(dir=dirname, suffix=".tmp")
        with os.fdopen(fd, "w") as f:
            f.write(content)
        os.replace(tmp_path, filepath)  # atômico no Linux
        return True
    except OSError:
        return False

def _save_decision(title: str, content: str) -> Optional[str]:
    today = datetime.now().strftime("%Y-%m-%d")
    slug = _sanitize_slug(title)
    filename = f"{today}-{slug}.md"
    filepath = os.path.join(DECISIONS_DIR, filename)

    note = f"""---
tags: [decision]
status: active
created: {today}
updated: {today}
source: hermes-session
---

# {title}

{content}
"""
    if _atomic_write(filepath, note):
        return filepath
    return None
```

### 1.4 — FIX: Slug Sanitization Robusta
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.2.1 — Slug não lida com caracteres especiais
**Solução**: Função de sanitização completa

```python
import re
import unicodedata

def _sanitize_slug(title: str, max_len: int = 60) -> str:
    """Sanitiza título para slug de arquivo seguro."""
    # Normaliza unicode (remove acentos, converte ç→c, etc.)
    text = unicodedata.normalize("NFKD", title)
    text = text.encode("ASCII", "ignore").decode("ASCII")
    # Substitui qualquer caractere não alfanumérico por hífen
    text = re.sub(r"[^a-zA-Z0-9]+", "-", text)
    # Remove hífens das bordas
    text = text.strip("-")
    # Trunca em boundary de palavra
    if len(text) > max_len:
        text = text[:max_len].rsplit("-", 1)[0]
    # Fallback se ficar vazio
    return text or "decision"
```

### 1.5 — FIX: Tratamento de Falhas de Escrita com Log
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.2.10 — `except OSError: pass` silencioso
**Solução**: Logar falhas em stderr

```python
import sys

def _save_decision(title: str, content: str) -> Optional[str]:
    # ... preparação ...
    if _atomic_write(filepath, note):
        return filepath
    print(f"[sinapse] ERROR: Failed to save decision '{title}' to {filepath}", file=sys.stderr)
    return None

def _save_learning(title: str, content: str) -> Optional[str]:
    today = datetime.now().strftime("%Y-%m-%d")
    entry = f"""

---

## {title} ({today})

{content}
"""
    try:
        with open(PATTERNS_FILE, "a") as f:
            f.write(entry)
        return PATTERNS_FILE
    except OSError as e:
        print(f"[sinapse] ERROR: Failed to save learning to {PATTERNS_FILE}: {e}", file=sys.stderr)
        return None
```

### 1.6 — FIX: Regex Frágil no Update Current State
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.2.3 — Regex não casa com quebra de linha
**Solução**: Usar flag MULTILINE + regex mais robusto

```python
import re  # mover para o topo do arquivo

def _update_current_state(decisions, learnings, summary):
    today = datetime.now().strftime("%Y-%m-%d %H:%M")
    os.makedirs(os.path.dirname(MEMORY_FILE), exist_ok=True)

    existing = ""
    try:
        with open(MEMORY_FILE, "r") as f:
            existing = f.read()
    except FileNotFoundError:
        pass

    # Build session block (existing logic)
    decision_lines = ""
    for d in decisions[-5:]:
        fname = os.path.basename(d).replace(".md", "")
        decision_lines += f"- Decisão: [[{fname}]]\n"

    learning_lines = ""
    for l in learnings[-5:]:
        fname = os.path.basename(l).replace(".md", "")
        learning_lines += f"- Aprendizado: [[{fname}]]\n"

    session_block = f"""
## Session: {today}

### Decisions
{decision_lines or '- Nenhuma decisão registrada'}
### Learnings
{learning_lines or '- Nenhum aprendizado registrado'}
### Summary
{summary[:500]}
"""
    # Update "Last Update" — usa MULTILINE para casar até fim da linha
    updated = existing
    updated = re.sub(
        r"^## Last Update:.*$",
        f"## Last Update: {today}",
        updated,
        flags=re.MULTILINE,
    )
    if "## Last Update:" not in updated:
        # Se não existia, insere no topo
        updated = f"## Last Update: {today}\n\n{updated}"

    updated += session_block

    if not _atomic_write(MEMORY_FILE, updated):
        print(f"[sinapse] ERROR: Failed to update {MEMORY_FILE}", file=sys.stderr)
```

---

## FASE 2 — Correções de Alta Prioridade

### 2.1 — FIX: Deduplicação de Aprendizados
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.2.2 — Conteúdo duplicado em Patterns.md
**Solução**: Verificar se aprendizado já existe antes de fazer append

```python
def _save_learning(title: str, content: str) -> Optional[str]:
    today = datetime.now().strftime("%Y-%m-%d")

    # Verifica duplicação (últimos 30 dias)
    try:
        with open(PATTERNS_FILE, "r") as f:
            existing = f.read()
        if title in existing:
            print(f"[sinapse] Learning '{title}' already exists — skipped", file=sys.stderr)
            return None
    except FileNotFoundError:
        pass

    entry = f"""

---

## {title} ({today})

{content}
"""
    try:
        with open(PATTERNS_FILE, "a") as f:
            f.write(entry)
        return PATTERNS_FILE
    except OSError as e:
        print(f"[sinapse] ERROR: Failed to save learning: {e}", file=sys.stderr)
        return None
```

### 2.2 — FIX: Set de Ferramentas Configurável
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.2.4 — DECISION_TOOLS hardcoded
**Solução**: Tornar configurável via variável de ambiente + manter fallback

```python
import os

# Decision tools: configurável via SINAPSE_DECISION_TOOLS env var
_DEFAULT_DECISION_TOOLS = {"memory_add", "observation_add", "mcp_claude_mem_memory_add"}
_custom_tools = os.environ.get("SINAPSE_DECISION_TOOLS", "")
if _custom_tools:
    DECISION_TOOLS = set(t.strip() for t in _custom_tools.split(","))
else:
    DECISION_TOOLS = _DEFAULT_DECISION_TOOLS
```

### 2.3 — FIX: Expansão dos Learning Signals
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.2.5 — Sinais limitados a pt/en
**Solução**: Expandir lista e permitir override

```python
_DEFAULT_LEARNING_SIGNALS = [
    # Português
    "aprendizado", "aprendizagem", "lição", "lição aprendida",
    "descoberta", "padrão identificado",
    # Inglês
    "learning", "insight", "pattern", "lesson",
    "lesson learned", "takeaway", "finding", "aha",
    "note to self", "tl;dr",
    # Espanhol (comum em times LATAM)
    "aprendizaje", "lección", "descubrimiento",
]

_custom_signals = os.environ.get("SINAPSE_LEARNING_SIGNALS", "")
if _custom_signals:
    LEARNING_SIGNALS = [s.strip().lower() for s in _custom_signals.split(",")]
else:
    LEARNING_SIGNALS = _DEFAULT_LEARNING_SIGNALS
```

### 2.4 — FIX: Normalização de Timeouts e Truncamentos
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.2.6 — 4 níveis de truncamento inconsistentes
**Solução**: Centralizar constantes e aplicar consistência

```python
# Timeouts e limites — centralizados e documentados
GLOBAL_QUERY_TIMEOUT = 8       # segundos — orçamento total
MAX_CONTEXT_CHARS = 3000        # limite de injeção no prompt
MAX_NODES = 5                   # máximo de nodes na resposta
MAX_OBSERVATIONS = 5            # máximo de observations
OBSERVATION_CHARS = 300         # caracteres por observation (consistente)
SEMANTIC_CONTEXT_CHARS = 500    # caracteres do contexto semântico
```

### 2.5 — FIX: sync_claude_mem_to_vault Usar API HTTP
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.2.9 — Bypassa API, usa SQLite direto
**Solução**: Usar a API HTTP de search

```python
def sync_claude_mem_to_vault():
    """Exporta observações recentes do claude-mem para o vault via API HTTP."""
    try:
        req = Request(
            f"{CLAUDE_MEM_URL}/api/search?query=&limit=10",
            method="GET",
        )
        with urlopen(req, timeout=CLAUDE_MEM_TIMEOUT) as resp:
            data = json.loads(resp.read().decode())
            items = data.get("items", [])
            for item in items:
                obs_id = item.get("id", "unknown")
                content = item.get("content") or item.get("excerpt", "")
                if content:
                    _save_decision(
                        title=f"claude-mem observation {obs_id}",
                        content=content,
                    )
    except (URLError, OSError, json.JSONDecodeError, ValueError) as e:
        print(f"[sinapse] claude-mem sync failed: {e}", file=sys.stderr)
```

### 2.6 — FIX: Parser NeuralMemory Mais Robusto
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.2.8 — Parser assume formato fixo de output
**Solução**: Adicionar fallback para JSON (se disponível), manter parser atual como último recurso

```python
def _backend_neural_memory(query: str) -> Optional[Dict[str, Any]]:
    if not os.path.isfile(NMEM_BIN) or not os.access(NMEM_BIN, os.X_OK):
        return None

    try:
        # Tenta formato JSON primeiro (mais recente do nmem)
        result = subprocess.run(
            [NMEM_BIN, "recall", "--json", query],
            capture_output=True,
            text=True,
            timeout=NMEM_TIMEOUT,
        )
        if result.returncode == 0 and result.stdout.strip():
            try:
                data = json.loads(result.stdout)
                memories = data if isinstance(data, list) else data.get("memories", [])
                if memories:
                    return {
                        "source": "neural-memory (associative)",
                        "observations": [
                            {"content": m.get("content", str(m)), "confidence": m.get("confidence", 0.5)}
                            for m in memories[:MAX_OBSERVATIONS]
                        ],
                        "count": len(memories),
                        "query": query,
                    }
            except json.JSONDecodeError:
                pass  # fallback para parser de texto
    except (subprocess.TimeoutExpired, FileNotFoundError, OSError):
        pass

    # Fallback: parser de texto (formato atual)
    try:
        result = subprocess.run(
            [NMEM_BIN, "recall", query],
            capture_output=True,
            text=True,
            timeout=NMEM_TIMEOUT,
        )
        if result.returncode != 0 or not result.stdout.strip():
            return None
        # ... parser de texto existente ...
    except (subprocess.TimeoutExpired, FileNotFoundError, OSError):
        return None
```

### 2.7 — FIX: Paths Hardcoded → Variáveis de Ambiente
**Arquivo**: `plugins/hermes/sinapse-memory.py` e scripts
**Gap**: A.1.7 — Paths absolutos no código
**Solução**: Resolver via `SINAPSE_HOME`

```python
# Plugin
SINAPSE_HOME = os.environ.get(
    "SINAPSE_HOME",
    os.path.expanduser("~/Documentos/Projects/sinapse_agent"),
)
```

```bash
# scripts/start-claude-mem.sh
SINAPSE_HOME="${SINAPSE_HOME:-$HOME/Documentos/Projects/sinapse_agent}"
export CLAUDE_MEM_DATA_DIR="$SINAPSE_HOME/claude-mem/data"
exec "$HOME/.bun/bin/bun" "$SINAPSE_HOME/claude-mem/plugin/scripts/mcp-server.cjs" "$@"
```

```bash
# scripts/serve-graph.sh
SINAPSE_HOME="${SINAPSE_HOME:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
GRAPH_PATH="$SINAPSE_HOME/cerebro/graphify-out/graph.json"
```

### 2.8 — FIX: Validação de Schema do graph.json
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.3.5 — Sem validação de schema
**Solução**: Validar estrutura mínima antes de usar

```python
def _validate_graph_schema(graph: dict) -> bool:
    """Valida que o graph.json tem a estrutura esperada."""
    if not isinstance(graph, dict):
        return False
    nodes = graph.get("nodes")
    links = graph.get("links")
    if not isinstance(nodes, list) or not isinstance(links, list):
        return False
    if nodes:
        required_keys = {"label", "id"}
        if not required_keys.issubset(nodes[0].keys()):
            return False
    return True

def _backend_graphify(query: str) -> Optional[Dict[str, Any]]:
    # ... load graph.json ...
    if not _validate_graph_schema(graph):
        print("[sinapse] graph.json schema invalid", file=sys.stderr)
        return None
    # ... resto da lógica ...
```

---

## FASE 3 — Melhorias de Design (Média Prioridade)

### 3.1 — Health Check Unificado
**Arquivo**: Novo `plugins/hermes/sinapse-memory.py` (adicionar função)
**Gap**: A.1.1 — Sem health check agregado
**Solução**: Expander `health_check()` existente

```python
def health_check() -> Dict[str, Any]:
    """Retorna status completo de todos os backends e componentes."""
    status = {
        "timestamp": datetime.now().isoformat(),
        "backends": {
            "neural_memory": _check_nmem(),
            "claude_mem": _check_claude_mem(),
            "graphify": _check_graphify(),
            "rtk": _check_rtk(),
        },
        "vault": {
            "path": VAULT_DIR,
            "exists": os.path.isdir(VAULT_DIR),
            "graph_nodes": _get_graph_node_count(),
        },
        "plugin": {
            "backends_registered": len(_READ_BACKENDS),
        },
    }
    status["healthy"] = all(
        v for v in status["backends"].values()
    )
    return status

def _check_nmem() -> bool:
    return os.path.isfile(NMEM_BIN) and os.access(NMEM_BIN, os.X_OK)

def _check_graphify() -> bool:
    return os.path.isfile(GRAPH_JSON)

def _check_rtk() -> bool:
    try:
        subprocess.run(["rtk", "--version"], capture_output=True, timeout=2)
        return True
    except Exception:
        return False

def _check_claude_mem() -> bool:
    try:
        req = Request(f"{CLAUDE_MEM_URL}/health", method="GET")
        with urlopen(req, timeout=2) as resp:
            data = json.loads(resp.read().decode())
            return data.get("status") == "ok"
    except Exception:
        return False

def _get_graph_node_count() -> int:
    try:
        with open(GRAPH_JSON) as f:
            return len(json.load(f).get("nodes", []))
    except Exception:
        return 0
```

### 3.2 — Cache de graph.json com TTL
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.3.3 — Lê graph.json do disco a cada consulta
**Solução**: Cache em memória com invalidação por mtime

```python
import time

_graph_cache: Dict[str, Any] = {}
_graph_cache_time: float = 0
_GRAPH_CACHE_TTL = 60  # segundos

def _load_graph() -> Optional[dict]:
    """Carrega graph.json com cache TTL + invalidação por mtime."""
    global _graph_cache, _graph_cache_time

    if not os.path.isfile(GRAPH_JSON):
        return None

    mtime = os.path.getmtime(GRAPH_JSON)
    now = time.time()

    if _graph_cache and (now - _graph_cache_time) < _GRAPH_CACHE_TTL:
        return _graph_cache

    try:
        with open(GRAPH_JSON, "r") as f:
            _graph_cache = json.load(f)
        _graph_cache_time = now
        return _graph_cache
    except (json.JSONDecodeError, OSError):
        return None
```

### 3.3 — Modo Dry-Run para Escrita
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.3.4 — Sem modo de teste sem side effects
**Solução**: Flag de ambiente

```python
DRY_RUN = os.environ.get("SINAPSE_DRY_RUN", "").lower() in ("1", "true", "yes")

def _save_decision(title: str, content: str) -> Optional[str]:
    if DRY_RUN:
        print(f"[sinapse] DRY-RUN: would save decision '{title}'")
        return "/dev/null/dry-run"
    # ... lógica normal ...
```

### 3.4 — Circuit Breaker Simplificado
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.1.2 — Backend lento penaliza todas as consultas
**Solução**: Marcar backend como "degraded" após N falhas consecutivas

```python
_backend_state: Dict[str, Dict[str, Any]] = {}

def _is_backend_healthy(name: str) -> bool:
    """Circuit breaker simples: pula backends com falhas recentes."""
    state = _backend_state.get(name, {})
    failures = state.get("failures", 0)
    last_failure = state.get("last_failure", 0)
    cooldown = state.get("cooldown", 30)

    if failures >= 3 and (time.time() - last_failure) < cooldown:
        return False
    return True

def _record_backend_result(name: str, success: bool):
    state = _backend_state.setdefault(name, {"failures": 0, "last_failure": 0, "cooldown": 30})
    if success:
        state["failures"] = 0
    else:
        state["failures"] += 1
        state["last_failure"] = time.time()
```

### 3.5 — Logs Estruturados
**Arquivo**: Novo `plugins/hermes/sinapse-memory.py` (adicionar)
**Gap**: A.1.6 — Sem observabilidade
**Solução**: Função de log que emite JSON estruturado opcionalmente

```python
import json as _json

_LOG_JSON = os.environ.get("SINAPSE_LOG_JSON", "").lower() in ("1", "true", "yes")

def _log(level: str, event: str, **kwargs):
    """Log estruturado opcional. Sempre loga em stderr."""
    if _LOG_JSON:
        entry = {"ts": datetime.now().isoformat(), "level": level, "event": event, **kwargs}
        print(_json.dumps(entry), file=sys.stderr)
    else:
        extra = " ".join(f"{k}={v}" for k, v in kwargs.items())
        print(f"[sinapse] {level.upper()}: {event} {extra}".strip(), file=sys.stderr)
```

### 3.6 — Configuração Centralizada via sinapse.yaml
**Arquivo**: Novo `plugins/hermes/sinapse-memory.py` (modificar init)
**Gap**: A.3.1 — Config dispersa
**Solução**: Carregar sinapse.yaml no startup

```python
import yaml  # ou implementar parser simples de YAML

def _load_config() -> dict:
    """Carrega sinapse.yaml se disponível."""
    config_path = os.path.join(SINAPSE_HOME, "sinapse.yaml")
    if os.path.isfile(config_path):
        try:
            with open(config_path) as f:
                return yaml.safe_load(f)
        except Exception:
            pass
    return {}

_config = _load_config()
```

### 3.7 — Orçamento de Tempo Global para Query
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: A.3.2 — Timeouts não coordenados
**Solução**: Wrapper com timeout global

```python
import signal

def _query_vault_knowledge(query: str) -> Optional[Dict[str, Any]]:
    if not query or not query.strip():
        return None

    deadline = time.time() + GLOBAL_QUERY_TIMEOUT

    for backend in _READ_BACKENDS:
        if time.time() > deadline:
            _log("warn", "query_timeout", query=query[:50])
            break

        remaining = deadline - time.time()
        if remaining <= 0:
            break

        try:
            # Cada backend tem seu próprio timeout (já configurado)
            result = backend(query)
            if result:
                _record_backend_result(backend.__name__, True)
                return result
            _record_backend_result(backend.__name__, False)
        except (URLError, OSError, subprocess.TimeoutExpired, FileNotFoundError):
            _record_backend_result(backend.__name__, False)
            continue
        except Exception as e:
            _log("error", "backend_error", backend=backend.__name__, error=str(e))
            _record_backend_result(backend.__name__, False)
            continue

    return None
```

### 3.8 — Validação de Frontmatter nos Arquivos Salvos
**Arquivo**: `plugins/hermes/sinapse-memory.py`
**Gap**: Salvamento não valida estrutura antes de escrever
**Solução**: Validar YAML frontmatter

```python
def _validate_frontmatter_yaml(content: str) -> bool:
    """Verifica se o frontmatter YAML é válido."""
    if not content.startswith("---"):
        return False
    try:
        parts = content.split("---", 2)
        if len(parts) < 3:
            return False
        yaml_content = parts[1].strip()
        # Validação básica: keys obrigatórias
        return all(k in yaml_content for k in ("tags:", "status:", "created:"))
    except Exception:
        return False
```

### 3.9 — Script de Disaster Recovery
**Arquivo**: Novo `scripts/recover.sh`
**Gap**: A.3.6 — Sem procedimento de disaster recovery documentado
**Solução**: Script automatizado

```bash
#!/bin/bash
# Sinapse Agent — Disaster Recovery
# Uso: bash scripts/recover.sh

set -euo pipefail
SINAPSE_HOME="${SINAPSE_HOME:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
echo "=== Sinapse Agent — Disaster Recovery ==="

# 1. Verificar graph.json
if [ ! -f "$SINAPSE_HOME/cerebro/graphify-out/graph.json" ]; then
    echo "[1/5] graph.json ausente — rebuild..."
    cd "$SINAPSE_HOME" && graphify update cerebro/ 2>&1
fi

# 2. Verificar backup
if [ -f "$SINAPSE_HOME/cerebro/graphify-out/graph.json.bak" ]; then
    echo "[2/5] Backup encontrado — verificando integridade..."
    python3 -c "import json; json.load(open('$SINAPSE_HOME/cerebro/graphify-out/graph.json.bak'))" 2>/dev/null && \
        echo "  backup válido" || echo "  backup corrompido"
fi

# 3. Reiniciar worker claude-mem
echo "[3/5] Reiniciando worker..."
systemctl --user restart sinapse-claude-mem.service 2>/dev/null || \
    echo "  systemd indisponível — inicie manualmente"

# 4. Health check
echo "[4/5] Health check..."
sleep 2
if curl -s --max-time 3 http://127.0.0.1:37700/health | grep -q '"status":"ok"'; then
    echo "  ✓ worker healthy"
else
    echo "  ✗ worker não respondeu"
fi

# 5. Verificar plugin
echo "[5/5] Verificando plugin..."
python3 -c "
import sys; sys.path.insert(0, '$SINAPSE_HOME/plugins/hermes')
from importlib import import_module
m = import_module('sinapse-memory')
status = m.health_check()
print(f'  Backends: {status[\"backends\"]}')
print(f'  Graph nodes: {status[\"vault\"][\"graph_nodes\"]}')
" 2>/dev/null || echo "  ✗ Plugin não carregou"

echo ""
echo "Recovery concluído."
```

### 3.10 — Suporte a SINAPSE_HOME em Todos os Scripts
**Arquivos**: `scripts/*.sh`, `cron/sync-diario.sh`
**Gap**: A.1.7 — Paths hardcoded nos scripts
**Solução**: Resolver via SINAPSE_HOME com fallback

```bash
# Template para todos os scripts
SINAPSE_HOME="${SINAPSE_HOME:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
```

---

## FASE 4 — Suite de Testes Unitários (41 testes)

**Objetivo**: Cobrir todas as funções isoladas do plugin com mocks.

**Arquivos a criar**:

```
tests/
├── conftest.py                      # Fixtures globais
├── fixtures/
│   ├── sample_graph.json            # Grafo mínimo (5 nodes, 3 edges)
│   ├── sample_vault/                # Vault com 5 notas
│   │   ├── brain/
│   │   │   ├── Current State.md
│   │   │   └── Patterns.md
│   │   ├── work/
│   │   │   └── active/
│   │   └── graphify-out/
│   │       └── graph.json
│   └── mock_servers/
│       └── claude_mem_mock.py       # Mock HTTP para testes U2.x
├── unit/
│   ├── conftest.py
│   ├── test_backend_graphify.py     # U1.1-U1.9
│   ├── test_backend_claude_mem.py   # U2.1-U2.7
│   ├── test_backend_nmem.py         # U3.1-U3.7
│   ├── test_write_helpers.py        # U4.1-U4.9
│   ├── test_query_engine.py         # U5.1-U5.5
│   └── test_format_context.py       # U6.1-U6.4
```

### Fixture: `tests/conftest.py`

```python
import os
import sys
import json
import tempfile
import shutil
from pathlib import Path
import pytest

# Add plugin to path
sys.path.insert(0, str(Path(__file__).parent.parent / "plugins" / "hermes"))


@pytest.fixture
def sample_graph():
    """Grafo mínimo para testes unitários."""
    return {
        "nodes": [
            {"label": "thoth", "file_type": "document", "source_file": "thoth.md", "id": "thoth", "community": 1, "norm_label": "thoth"},
            {"label": "vps", "file_type": "document", "source_file": "vps.md", "id": "vps", "community": 2, "norm_label": "vps"},
            {"label": "pricing", "file_type": "document", "source_file": "pricing.md", "id": "pricing", "community": 3, "norm_label": "pricing"},
            {"label": "deploy", "file_type": "code", "source_file": "deploy.sh", "id": "deploy", "community": 2, "norm_label": "deploy"},
            {"label": "database", "file_type": "code", "source_file": "db.py", "id": "database", "community": 4, "norm_label": "database"},
        ],
        "links": [
            {"source": "thoth", "target": "vps", "relation": "related_to", "confidence": "EXTRACTED", "source_file": "thoth.md", "source_location": "L5", "weight": 1.0, "confidence_score": 1.0},
            {"source": "vps", "target": "deploy", "relation": "managed_by", "confidence": "EXTRACTED", "source_file": "vps.md", "source_location": "L10", "weight": 1.0, "confidence_score": 1.0},
            {"source": "database", "target": "deploy", "relation": "depends_on", "confidence": "EXTRACTED", "source_file": "db.py", "source_location": "L3", "weight": 1.0, "confidence_score": 1.0},
        ],
    }


@pytest.fixture
def temp_vault():
    """Vault temporário isolado para testes de escrita."""
    tmp = tempfile.mkdtemp()
    os.makedirs(f"{tmp}/work/active", exist_ok=True)
    os.makedirs(f"{tmp}/brain", exist_ok=True)
    os.makedirs(f"{tmp}/graphify-out", exist_ok=True)
    yield tmp
    shutil.rmtree(tmp, ignore_errors=True)
```

### Suíte U1: `tests/unit/test_backend_graphify.py`

```python
import json
import os
import pytest
from sinapse_memory import _backend_graphify, register_backend

# NOTE: Precisamos extrair _backend_graphify para teste.
# Solução: criar wrapper testável ou usar importlib para acessar função interna.
# Alternativa: refatorar plugin para expor funções testáveis como módulo.

# Abordagem pragmática: adicionar bloco if __name__ != "__main__" no plugin
# que expõe __all__ com as funções testáveis.

class TestBackendGraphify:
    def test_graph_json_missing(self, monkeypatch):
        """U1.1: graph.json ausente retorna None."""
        monkeypatch.setattr("sinapse_memory.GRAPH_JSON", "/nonexistent/path.json")
        result = _backend_graphify("query")
        assert result is None

    def test_graph_json_corrupted(self, tmp_path):
        """U1.2: JSON inválido retorna None."""
        corrupt = tmp_path / "graph.json"
        corrupt.write_text("NOT VALID JSON {{{")
        # monkeypatch GRAPH_JSON para apontar para arquivo corrompido
        ...
        result = _backend_graphify("query")
        assert result is None

    def test_empty_query(self):
        """U1.3: Query vazia não causa erro."""
        result = _backend_graphify("")
        assert result is None

    def test_exact_match_found(self, sample_graph, tmp_path):
        """U1.4: Match exato retorna nodes."""
        gpath = tmp_path / "graph.json"
        gpath.write_text(json.dumps(sample_graph))
        ...
        result = _backend_graphify("thoth")
        assert result is not None
        assert len(result["nodes"]) > 0
        assert result["nodes"][0]["label"] == "thoth"

    def test_no_match(self, sample_graph, tmp_path):
        """U1.5: Query sem match retorna None."""
        ...
        result = _backend_graphify("xyznonexistent123")
        assert result is None

    def test_edge_match(self, sample_graph, tmp_path):
        """U1.6: Match em edge retorna edges."""
        ...
        result = _backend_graphify("related_to")
        assert result is not None
        assert len(result["edges"]) > 0

    def test_max_nodes_limit(self, sample_graph, tmp_path):
        """U1.7: Respeita MAX_NODES."""
        ...
        # Cria 20 nodes que dariam match
        for i in range(20):
            sample_graph["nodes"].append({
                "label": f"thoth_{i}", "file_type": "doc",
                "source_file": f"t{i}.md", "id": f"thoth_{i}",
                "community": 1, "norm_label": f"thoth_{i}"
            })
        ...
        result = _backend_graphify("thoth")
        assert len(result["nodes"]) <= 5

    def test_normalization_cross_idioma(self, sample_graph, tmp_path):
        """U1.8: Normalização lida com acentos."""
        # Adiciona node com label acentuado
        sample_graph["nodes"].append({
            "label": "João Ação", ...
        })
        ...
        result = _backend_graphify("joao acao")
        assert result is not None

    def test_score_ordering(self, sample_graph, tmp_path):
        """U1.9: Nodes ordenados por score decrescente."""
        ...
        result = _backend_graphify("thoth")
        scores = [n["score"] for n in result["nodes"]]
        assert scores == sorted(scores, reverse=True)
```

### Demais suítes unitárias

As suítes U2-U6 seguem o mesmo padrão — testam cada função isoladamente com mocks para:
- `urllib.request` (claude-mem HTTP)
- `subprocess.run` (nmem CLI)
- Sistema de arquivos temporário (escrita)
- `sinapse_memory.MAX_NODES`, `MAX_OBSERVATIONS`, `MAX_CONTEXT_CHARS`

---

## FASE 5 — Testes de Integração e End-to-End (42 testes)

```
tests/
├── integration/
│   ├── conftest.py            # Fixtures com backends reais
│   ├── test_read_flow.py      # I1.1-I1.5
│   ├── test_write_read_cycle.py # I2.1-I2.4
│   ├── test_mcp.py            # I3.1-I3.3
│   └── test_cron.py           # I4.1-I4.3
└── e2e/
    ├── conftest.py            # Fixtures com sistema completo
    ├── test_full_session.py   # E1
    ├── test_degradation.py    # E2
    ├── test_concurrency.py    # E3
    ├── test_recovery.py       # E4
    └── test_edge_cases.py     # E5
```

### Integration: `tests/integration/conftest.py`

```python
import pytest
import subprocess
import time
import os

@pytest.fixture(scope="module")
def ensure_backends():
    """Garante que backends reais estão operacionais."""
    # Verifica claude-mem
    import urllib.request, json
    try:
        req = urllib.request.Request("http://127.0.0.1:37700/health")
        with urllib.request.urlopen(req, timeout=3) as resp:
            assert json.loads(resp.read()).get("status") == "ok"
    except Exception:
        pytest.skip("claude-mem worker not available")

    # Verifica nmem
    if not os.path.isfile(os.path.expanduser("~/.local/bin/nmem")):
        pytest.skip("nmem not installed")

    return True
```

### E2E: `tests/e2e/test_full_session.py`

```python
class TestFullSession:
    """E1: Ciclo completo de sessão."""

    def test_full_read_write_cycle(self, temp_vault, monkeypatch):
        """Simula sessão completa: pre_prompt → post_tool → post_session."""
        from sinapse_memory import _pre_prompt_build, _post_tool_use, _post_session_end
        import sinapse_memory as sm

        # Redireciona paths para vault temporário
        monkeypatch.setattr(sm, "VAULT_DIR", temp_vault)
        monkeypatch.setattr(sm, "DECISIONS_DIR", f"{temp_vault}/work/active")
        monkeypatch.setattr(sm, "MEMORY_FILE", f"{temp_vault}/brain/Current State.md")
        monkeypatch.setattr(sm, "PATTERNS_FILE", f"{temp_vault}/brain/Patterns.md")

        # Passo 1: Leitura (simplificado — sem backends reais)
        result = _pre_prompt_build(user_message="projeto thoth")
        # Em ambiente de teste sem backends, pode não injetar contexto
        # Mas não deve crashar
        assert isinstance(result, dict)

        # Passo 2: Escrita de decisão
        _post_tool_use(
            tool_name="memory_add",
            tool_args={
                "title": "Migrar servidor para Hetzner",
                "content": "Decisão: migrar VPS para Hetzner. Insight: padrão de economizar em cloud.",
            },
        )

        # Verifica decisão salva
        decisions = os.listdir(f"{temp_vault}/work/active")
        assert any("migrar-servidor" in d for d in decisions)

        # Passo 3: Fim de sessão
        _post_session_end(session_summary="Sessão produtiva com decisões importantes")

        # Verifica Current State
        with open(f"{temp_vault}/brain/Current State.md") as f:
            state = f.read()
        assert "Migrar servidor" in state or "migrar-servidor" in state
        assert "Last Update:" in state

        # Verifica aprendizado (Patterns.md)
        with open(f"{temp_vault}/brain/Patterns.md") as f:
            patterns = f.read()
        assert "Migrar servidor" in patterns
```

### E2E: `tests/e2e/test_degradation.py`

```python
class TestGracefulDegradation:
    """E2: Sistema funciona com backends falhando."""

    def test_all_backends_fail_gracefully(self, monkeypatch):
        """Todos backends offline → não crasha."""
        from sinapse_memory import _query_vault_knowledge

        # Simula todos backends como None
        def mock_fail(query):
            return None

        monkeypatch.setattr("sinapse_memory._READ_BACKENDS", [mock_fail, mock_fail, mock_fail])
        result = _query_vault_knowledge("test")
        assert result is None  # Não crasha

    def test_second_backend_kicks_in(self, monkeypatch):
        """Primeiro backend falha, segundo responde."""
        def fail_first(query):
            return None

        def succeed_second(query):
            return {"source": "mock", "observations": [{"content": "found"}]}

        monkeypatch.setattr("sinapse_memory._READ_BACKENDS", [fail_first, succeed_second])
        result = _query_vault_knowledge("test")
        assert result is not None
        assert result["source"] == "mock"

    def test_bug_in_backend_doesnt_crash(self, monkeypatch):
        """Backend com bug (KeyError) não crasha o sistema."""
        def buggy(query):
            raise KeyError("simulated bug")

        def fallback(query):
            return {"source": "fallback", "observations": []}

        monkeypatch.setattr("sinapse_memory._READ_BACKENDS", [buggy, fallback])
        result = _query_vault_knowledge("test")
        assert result is not None
        assert result["source"] == "fallback"
```

---

## FASE 6 — CI/CD e Documentação

### 6.1 — GitHub Actions Workflow

**Arquivo**: `.github/workflows/test.yml`

```yaml
name: Sinapse Agent Test Suite

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * *"  # Teste diário às 6am

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          pip install --user graphify
          pip install --user neural-memory
          sudo apt-get install -y sqlite3
      - name: Smoke tests
        run: bash tests/smoke/test_smoke.sh
        env:
          SINAPSE_HOME: ${{ github.workspace }}

  unit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install test deps
        run: pip install pytest pytest-timeout pytest-mock
      - name: Unit tests
        run: python3 -m pytest tests/unit/ -v --tb=short
        env:
          SINAPSE_HOME: ${{ github.workspace }}

  integration:
    needs: [smoke, unit]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Setup full environment
        run: |
          bash install.sh --force --skip-agent=hermes
      - name: Integration tests
        run: python3 -m pytest tests/integration/ -v --tb=short --run-integration
        env:
          SINAPSE_HOME: ${{ github.workspace }}

  e2e:
    needs: [integration]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Setup full environment
        run: |
          bash install.sh --force --skip-agent=hermes
      - name: End-to-end tests
        run: python3 -m pytest tests/e2e/ -v --tb=short --run-e2e
        env:
          SINAPSE_HOME: ${{ github.workspace }}
          SINAPSE_DRY_RUN: "1"
```

### 6.2 — Script de Execução de Testes Completo

**Arquivo**: `tests/run_all.sh`

```bash
#!/bin/bash
set -euo pipefail
SINAPSE_HOME="${SINAPSE_HOME:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
export SINAPSE_HOME
cd "$SINAPSE_HOME"

PASS=0; FAIL=0
run_suite() {
    local name="$1"; shift
    echo ""
    echo "════════════════════════════════════════════════════"
    echo "  SUITE: $name"
    echo "════════════════════════════════════════════════════"
    if "$@"; then
        echo "  ✓ $name PASSED"
        ((PASS++))
    else
        echo "  ✗ $name FAILED"
        ((FAIL++))
    fi
}

run_suite "S0 — Smoke" bash tests/smoke/test_smoke.sh
run_suite "U — Unit" python3 -m pytest tests/unit/ -v --tb=short
run_suite "I — Integration" python3 -m pytest tests/integration/ -v --tb=short --run-integration
run_suite "E — End-to-End" python3 -m pytest tests/e2e/ -v --tb=short --run-e2e

echo ""
echo "════════════════════════════════════════════════════"
echo "  RESULTS: $PASS suites passed, $FAIL suites failed"
echo "════════════════════════════════════════════════════"
[ "$FAIL" -eq 0 ] || exit 1
```

### 6.3 — Atualização do ARCHITECTURE.md

Adicionar seção "12. Testes e Qualidade" e "13. Disaster Recovery" com:

- Instruções de execução da suite de testes
- Matriz de cobertura
- Procedimento de recovery
- Health check unificado

### 6.4 — Atualização do AGENTS.md

Adicionar seção "6. Testes" com:

```markdown
## 6. Testes

Antes de qualquer commit, execute:

```bash
./tests/run_all.sh
```

Se a suite completa for muito longa, execute ao menos os smoke tests:

```bash
bash tests/smoke/test_smoke.sh
```
```

### 6.5 — Arquivo `tests/README.md`

Documentar:
- Estrutura das suites
- Como rodar cada nível
- Como adicionar novos testes
- Fixtures disponíveis
- Convenções de nomenclatura

---

## Cronograma de Execução

| Ordem | Fase | Entregáveis | Depende de |
|-------|------|-------------|-----------|
| 1 | Fase 1 | 6 correções críticas (plugin + scripts) | — |
| 2 | Fase 2 | 8 correções de alta prioridade | Fase 1 |
| 3 | Fase 3 | 10 melhorias de design | Fase 1 |
| 4 | Fase 4 | 41 testes unitários + fixtures | Fase 1 (plugin precisa ser importável) |
| 5 | Fase 5 | 42 testes integração + E2E | Fases 2, 3, 4 |
| 6 | Fase 6 | CI/CD + docs + smoke script | Fase 4 |

**Ordem sugerida de implementação**:
1. Primeiro, refatorar `sinapse-memory.py` para ser importável como módulo (expor `__all__`)
2. Implementar Fase 1 (correções críticas)
3. Criar fixtures e smoke tests (Fase 0 + infra da Fase 4)
4. Implementar Fase 2 (correções altas)
5. Implementar Fase 3 (melhorias)
6. Completar Fase 4 (testes unitários)
7. Completar Fase 5 (testes integração/E2E)
8. Fase 6 (CI/CD + docs)

---

## Checklist de Verificação Final

- [ ] `bash tests/smoke/test_smoke.sh` → 100% pass
- [ ] `python3 -m pytest tests/unit/ -v` → 41/41 pass
- [ ] `python3 -m pytest tests/integration/ -v --run-integration` → 23/23 pass
- [ ] `python3 -m pytest tests/e2e/ -v --run-e2e` → 19/19 pass
- [ ] `./scripts/build-graph.sh` → graph.json válido com backup
- [ ] `./scripts/recover.sh` → executa sem erro
- [ ] `curl http://127.0.0.1:37700/health` → `{"status":"ok"}`
- [ ] Plugin carrega sem erros: `python3 -c "from sinapse_memory import health_check; print(health_check())"`
- [ ] `SINAPSE_HOME=/tmp/test` não quebra scripts
- [ ] Decisão com emoji no título gera arquivo válido
- [ ] Aprendizado duplicado não é salvo 2x
- [ ] graph.json corrompido → rebuild automático no próximo cron
- [ ] CI/CD (GitHub Actions) passa em push
