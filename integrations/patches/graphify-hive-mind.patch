diff --git a/graphify/__main__.py b/graphify/__main__.py
index 157a48a61856..597baabc18be 100644
--- a/graphify/__main__.py
+++ b/graphify/__main__.py
@@ -3387,7 +3387,7 @@ def main() -> None:
         )
         from graphify.report import generate
         from graphify.export import to_json, to_html
-
+        from graphify.sqlite_export import to_sqlite
         print("Loading existing graph...")
         # Solution 3 (#1019): don't hard-exit on an oversized graph.json here.
         # Core outputs (graph.json + GRAPH_REPORT.md) still get written; the
@@ -3464,6 +3464,12 @@ def main() -> None:
         from graphify.export import backup_if_protected as _backup
         _backup(out)
         to_json(G, communities, str(out / "graph.json"), community_labels=labels)
+        try:
+            sinapse_home = os.environ.get("SINAPSE_HOME", str(watch_path.parent if watch_path.name == "cerebro" else watch_path))
+            db_path = os.path.join(sinapse_home, "hive_mind.db")
+            to_sqlite(G, communities, db_path)
+        except Exception as e:
+            print(f"[umc] Warning: failed to export to SQLite: {e}")
         labels_path.write_text(json.dumps({str(k): v for k, v in labels.items()}, ensure_ascii=False), encoding="utf-8")
 
         # Mirror watch.py pattern: gate to_html so core outputs (graph.json +
@@ -4697,6 +4703,7 @@ def main() -> None:
         )
         from graphify.cluster import cluster as _cluster, score_all as _score_all
         from graphify.export import to_json as _to_json
+        from graphify.sqlite_export import to_sqlite as _to_sqlite
         from graphify.analyze import god_nodes as _god_nodes, surprising_connections as _surprising
         dedup_backend = backend if dedup_llm else None
         if incremental_mode:
@@ -4733,6 +4740,10 @@ def main() -> None:
         from graphify.export import backup_if_protected as _backup
         _backup(graphify_out)
         _to_json(G, communities, str(graph_json_path), force=True)
+        try:
+            _to_sqlite(G, communities, str(target / "hive_mind.db"))
+        except Exception as e:
+            print(f"[umc] Warning: failed to export to SQLite: {e}")
         if merged.get("output_tokens", 0) > 0:
             (graphify_out / ".graphify_semantic_marker").write_text(
                 json.dumps({"output_tokens": merged["output_tokens"]}), encoding="utf-8"
diff --git a/graphify/extract.py b/graphify/extract.py
index a618e68abd7e..90a0a90ee876 100644
--- a/graphify/extract.py
+++ b/graphify/extract.py
@@ -9668,6 +9668,9 @@ def extract_markdown(path: Path) -> dict:
 
     file_nid = _make_id(str(path))
     add_node(file_nid, path.name, 1)
+    # Hive-Mind: Store raw content in the file node for SQLite/VEC export
+    if nodes and nodes[0]["id"] == file_nid:
+        nodes[0]["content"] = source
 
     source_dir = path.parent
     # Dedup link edges by resolved target node so a hub doc that links to the
diff --git a/graphify/sqlite_export.py b/graphify/sqlite_export.py
new file mode 100644
index 000000000000..3a092ada41a7
--- /dev/null
+++ b/graphify/sqlite_export.py
@@ -0,0 +1,142 @@
+import sqlite3
+import json
+import os
+import hashlib
+import sys
+from pathlib import Path
+import networkx as nx
+
+# Importa o core do UMC
+SINAPSE_HOME = os.environ.get("SINAPSE_HOME", str(Path(__file__).resolve().parent.parent.parent.parent))
+sys.path.append(SINAPSE_HOME)
+try:
+    from core.database import serialize_f32, get_embedder
+    from core.indexing import upsert_search_vec
+    import sqlite_vec
+except ImportError:
+    serialize_f32 = get_embedder = upsert_search_vec = sqlite_vec = None
+
+
+def _embedding_to_list(vec):
+    """Normalize fastembed/numpy vectors and Ollama list vectors."""
+    return vec.tolist() if hasattr(vec, "tolist") else list(vec)
+
+def to_sqlite(G: nx.Graph, communities: dict[int, list[str]], db_path: str):
+    """Exporta o grafo NetworkX para o banco de dados SQLite UMC."""
+    print(f"[umc] Conectando ao banco em: {os.path.abspath(db_path)}")
+    if not serialize_f32 or not get_embedder or not sqlite_vec:
+        print("[umc] Erro: core.database não encontrado.")
+        return
+
+    conn = sqlite3.connect(db_path, timeout=30)
+    conn.row_factory = sqlite3.Row
+    conn.execute("PRAGMA foreign_keys=ON")
+    conn.execute("PRAGMA busy_timeout=10000")
+    conn.enable_load_extension(True)
+    try:
+        sqlite_vec.load(conn)
+    finally:
+        conn.enable_load_extension(False)
+    cursor = conn.cursor()
+
+    # Mapeamento de comunidades
+    node_community = {}
+    for comm_id, nodes in communities.items():
+        for node_id in nodes:
+            node_community[node_id] = comm_id
+
+    embedder = get_embedder()
+
+    # Sincronizar Neurônios (Nodes)
+    print(f"[umc] Sincronizando {G.number_of_nodes()} neurônios...")
+    for node_id, data in G.nodes(data=True):
+        label = data.get("label", node_id)
+        node_type = data.get("file_type", "unknown")
+        source_file = data.get("source_file", "")
+        content = data.get("content", "")
+
+        # Se o conteúdo estiver vazio mas houver source_file, tenta ler do disco
+        if not content and source_file:
+            try:
+                candidates = (
+                    os.path.join(SINAPSE_HOME, source_file),
+                    os.path.join(SINAPSE_HOME, "cerebro", source_file),
+                )
+                for full_path in candidates:
+                    if os.path.isfile(full_path):
+                        with open(full_path, "r", encoding="utf-8") as f:
+                            content = f.read()
+                        break
+            except Exception:
+                pass
+
+        comm = node_community.get(node_id, 0)
+
+        # Calcula hash do conteúdo
+        embedding_text = content or label
+        content_hash = hashlib.sha256(embedding_text.encode()).hexdigest()
+
+        metadata = json.dumps({k: v for k, v in data.items() if k not in ["label", "file_type", "source_file", "content"]})
+
+        previous = cursor.execute(
+            "SELECT hash FROM neurons WHERE id = ?", (node_id,)
+        ).fetchone()
+        content_changed = previous is None or previous["hash"] != content_hash
+
+        cursor.execute("""
+            INSERT INTO neurons (id, label, type, source_file, content, hash, community, metadata, updated_at)
+            VALUES (?, ?, ?, ?, ?, ?, ?, ?, CURRENT_TIMESTAMP)
+            ON CONFLICT(id) DO UPDATE SET
+                label=excluded.label,
+                type=excluded.type,
+                source_file=excluded.source_file,
+                content=excluded.content,
+                hash=excluded.hash,
+                community=excluded.community,
+                metadata=excluded.metadata,
+                indexed_at=CASE
+                    WHEN neurons.hash IS NOT excluded.hash THEN NULL
+                    ELSE neurons.indexed_at
+                END,
+                updated_at=CURRENT_TIMESTAMP
+        """, (node_id, label, node_type, source_file, content, content_hash, comm, metadata))
+
+        # Gera e salva embedding se houver conteúdo e embedder disponível
+        if embedding_text and embedder:
+            try:
+                cursor.execute(
+                    "SELECT neuron_id FROM search_vec WHERE neuron_id = ?", (node_id,)
+                )
+                if content_changed or not cursor.fetchone():
+                    vec = _embedding_to_list(list(embedder.embed(embedding_text[:5000]))[0])
+                    upsert_search_vec(conn, node_id, serialize_f32(vec))
+            except Exception as e:
+                # Circuit breaker do embedder (ex.: Ollama ausente): desliga a
+                # vetorização do resto do export em vez de pagar retry por nó.
+                if e.__class__.__name__ == "EmbedderOffline" or getattr(embedder, "offline", False):
+                    print(
+                        "[umc] Warning: embedding endpoint offline — "
+                        "pulando embeddings dos nós restantes (grafo estrutural segue completo).",
+                        file=sys.stderr,
+                    )
+                    embedder = None
+                else:
+                    print(f"[umc] Warning: embedding failed for {node_id}: {e}", file=sys.stderr)
+
+    # Sincronizar Sinapses (Edges)
+    print(f"[umc] Sincronizando {G.number_of_edges()} sinapses...")
+    cursor.execute("DELETE FROM synapses")
+
+    for u, v, data in G.edges(data=True):
+        relation = data.get("relation", "connected")
+        weight = data.get("weight", 1.0)
+        metadata = json.dumps({k: v for k, v in data.items() if k not in ["relation", "weight"]})
+
+        cursor.execute("""
+            INSERT INTO synapses (source_id, target_id, relation, weight, metadata)
+            VALUES (?, ?, ?, ?, ?)
+        """, (u, v, relation, weight, metadata))
+
+    conn.commit()
+    conn.close()
+    print("[umc] Sincronização estrutural e vetorial concluída.")
diff --git a/graphify/watch.py b/graphify/watch.py
index 046857b86acd..d10430a8a67d 100644
--- a/graphify/watch.py
+++ b/graphify/watch.py
@@ -4,6 +4,7 @@ import contextlib
 import json
 import os
 import re
+import subprocess
 import sys
 import time
 from pathlib import Path
@@ -196,9 +197,50 @@ from graphify.detect import (
     _is_ignored,
 )
 
-_WATCHED_EXTENSIONS = CODE_EXTENSIONS | DOC_EXTENSIONS | PAPER_EXTENSIONS | IMAGE_EXTENSIONS
+_WATCHED_EXTENSIONS = CODE_EXTENSIONS | DOC_EXTENSIONS | PAPER_EXTENSIONS | IMAGE_EXTENSIONS | {".docx"}
 _CODE_EXTENSIONS = CODE_EXTENSIONS
 
+# Extensions that trigger Hive-Mind media/document capture pipelines
+_HIVE_IMAGE_EXTENSIONS = {".png", ".jpg", ".jpeg", ".gif"}
+_HIVE_DOC_EXTENSIONS = {".pdf", ".docx"}
+
+# Debounce tracking: {absolute_path_str: last_processed_monotonic_time}
+_hive_capture_debounce: dict[str, float] = {}
+_HIVE_DEBOUNCE_SECONDS = 30.0
+
+
+def _trigger_document_ingest(file_path: Path) -> None:
+    """Call document_ingest.py --file <path> for a single PDF or DOCX file.
+
+    Uses subprocess.run with check=False so a capture failure never crashes
+    the watcher. Applies a 30-second per-file debounce.
+    """
+    abs_path = str(file_path.resolve())
+    now = time.monotonic()
+    if now - _hive_capture_debounce.get(abs_path, 0.0) < _HIVE_DEBOUNCE_SECONDS:
+        return
+    _hive_capture_debounce[abs_path] = now
+
+    script = Path(__file__).resolve().parent.parent.parent / "scripts" / "document_ingest.py"
+    if not script.exists():
+        print(f"[graphify watch] document_ingest.py not found at {script}", file=sys.stderr)
+        return
+
+    print(f"[graphify watch] [hive] Ingesting document: {file_path.name}")
+    result = subprocess.run(
+        [sys.executable, str(script), "--file", abs_path],
+        check=False,
+        capture_output=True,
+        text=True,
+        env={**os.environ, "SINAPSE_HOME": os.environ.get("SINAPSE_HOME", str(script.parent.parent))},
+    )
+    if result.returncode != 0:
+        print(
+            f"[graphify watch] [hive] document_ingest failed for {file_path.name}: "
+            f"{result.stderr.strip()}",
+            file=sys.stderr,
+        )
+
 
 def _report_root_label(watch_path: Path) -> str:
     if watch_path.is_absolute():
@@ -510,6 +552,7 @@ def _rebuild_code(
         from graphify.analyze import god_nodes, surprising_connections, suggest_questions
         from graphify.report import generate
         from graphify.export import to_json, to_html
+        from graphify.sqlite_export import to_sqlite
         from graphify.security import check_graph_file_size_cap
 
         detected = detect(watch_path, follow_symlinks=follow_symlinks)
@@ -755,7 +798,22 @@ def _rebuild_code(
                 flag = out / "needs_update"
                 if flag.exists():
                     flag.unlink()
-                print("[graphify watch] No code-graph topology changes detected; outputs left untouched.")
+
+                # Hive-Mind: Sync to SQLite even if topology is same (content might have changed)
+                try:
+                    sinapse_home = os.environ.get("SINAPSE_HOME", str(watch_root.parent if watch_root.name == "cerebro" else watch_root))
+                    db_path = os.path.join(sinapse_home, "hive_mind.db")
+                    # Use existing_graph_data as communities source if same_topology
+                    prev_communities = _node_community_map(existing_graph_data)
+                    # Reconstruct communities dict from map
+                    comm_dict = {}
+                    for nid, cid in prev_communities.items():
+                        comm_dict.setdefault(cid, []).append(nid)
+                    to_sqlite(G, comm_dict, db_path)
+                except Exception as e:
+                    print(f"[umc] Warning: failed to incremental export to SQLite: {e}")
+
+                print("[graphify watch] No code-graph topology changes detected; outputs left untouched (UMC synced).")
                 return True
 
         communities = cluster(G)
@@ -783,6 +841,12 @@ def _rebuild_code(
         labels_json = json.dumps({str(k): v for k, v in sorted(labels.items())}, ensure_ascii=False, indent=2) + "\n"
         graph_tmp = out / ".graph.tmp.json"
         json_written = to_json(G, communities, str(graph_tmp), force=True, built_at_commit=commit)
+        try:
+            sinapse_home = os.environ.get("SINAPSE_HOME", str(watch_root.parent if watch_root.name == "cerebro" else watch_root))
+            db_path = os.path.join(sinapse_home, "hive_mind.db")
+            to_sqlite(G, communities, db_path)
+        except Exception as e:
+            print(f"[umc] Warning: failed to export to SQLite: {e}")
         if not json_written:
             return False
         candidate_graph_data = json.loads(graph_tmp.read_text(encoding="utf-8"))
@@ -940,7 +1004,12 @@ def watch(watch_path: Path, debounce: float = 3.0) -> None:
             nonlocal last_trigger, pending
             if event.is_directory:
                 return
-            path = Path(os.fsdecode(event.src_path))
+            # Filter out read/access events (opened, closed_no_write, etc.)
+            if event.event_type not in ("modified", "created", "deleted", "moved"):
+                return
+            # Handle atomic save renames correctly
+            raw = event.dest_path if event.event_type == "moved" else event.src_path
+            path = Path(os.fsdecode(raw))
             # Check .graphifyignore BEFORE the extension/dotfile/out filters so
             # the cheapest short-circuit for users with broad ignore patterns
             # (node_modules/, .venv/, build/, …) fires first. _is_ignored
@@ -981,11 +1050,30 @@ def watch(watch_path: Path, debounce: float = 3.0) -> None:
                 batch = list(changed)
                 changed.clear()
                 print(f"\n[graphify watch] {len(batch)} file(s) changed")
+
+                # Hive-Mind: trigger media/document capture pipelines for files inside cerebro/
+                # visual_capture.py is a screen-capture tool (takes screenshots, not file paths)
+                # and cannot process existing image files — image trigger is skipped.
+                # document_ingest.py supports --file for per-file ingestion of PDF/DOCX.
+                for p in batch:
+                    try:
+                        p.relative_to(watch_path)
+                    except ValueError:
+                        continue
+                    ext = p.suffix.lower()
+                    if ext in _HIVE_DOC_EXTENSIONS and p.exists():
+                        _trigger_document_ingest(p)
+
                 has_non_code = _has_non_code(batch)
+                # Hive-Mind: treat docs with extractors (like .md) as code for real-time sync
                 has_code = any(p.suffix.lower() in _CODE_EXTENSIONS for p in batch)
+                if not has_code:
+                    from graphify.extract import _get_extractor
+                    has_code = any(_get_extractor(p) is not None for p in batch)
+
                 if has_code:
-                    _rebuild_code(watch_path)
-                if has_non_code:
+                    _rebuild_code(watch_path, changed_paths=batch, force=True)
+                elif has_non_code:
                     _notify_only(watch_path)
     except KeyboardInterrupt:
         print("\n[graphify watch] Stopped.")
diff --git a/tests/test_watch.py b/tests/test_watch.py
index f1b845125eca..86760f994ffc 100644
--- a/tests/test_watch.py
+++ b/tests/test_watch.py
@@ -4,6 +4,7 @@ import os
 import subprocess
 import sys
 import time
+import json
 from pathlib import Path
 import pytest
 
@@ -407,6 +408,32 @@ def test_rebuild_code_skips_cluster_when_topology_unchanged(tmp_path, monkeypatc
     assert calls["n"] == 1
 
 
+def test_incremental_rebuild_removes_deleted_extractor_nodes(tmp_path):
+    from graphify.watch import _rebuild_code
+
+    src = tmp_path / "deleted_later.py"
+    src.write_text("def removed_symbol():\n    return 1\n", encoding="utf-8")
+    assert _rebuild_code(tmp_path, changed_paths=[src], force=True)
+
+    graph_path = tmp_path / "graphify-out" / "graph.json"
+    graph = json.loads(graph_path.read_text(encoding="utf-8"))
+    graph["nodes"].append({
+        "id": "semantic-rationale",
+        "label": "Preserved rationale",
+        "file_type": "rationale",
+        "source_file": "semantic-rationale",
+    })
+    graph_path.write_text(json.dumps(graph), encoding="utf-8")
+
+    src.unlink()
+    (tmp_path / "remaining.py").write_text("def kept_symbol():\n    return 2\n", encoding="utf-8")
+    assert _rebuild_code(tmp_path, changed_paths=[src], force=True)
+
+    rebuilt = json.loads(graph_path.read_text(encoding="utf-8"))
+    assert not any(n.get("source_file") == "deleted_later.py" for n in rebuilt["nodes"])
+    assert any(n.get("id") == "semantic-rationale" for n in rebuilt["nodes"])
+
+
 # --- .graphifyignore honored in watch handler (gh-928) ---
 
 
