# Model Gateway environment variables (Priority 1 — Model Gateway + Capability Registry)
#
# .env.example could not be edited in the session that built this feature
# (directory-level permission deny on that exact file, both Read and Bash).
# This file is the canonical, tracked source of the Model Gateway env block —
# copy these lines into your real .env (or into .env.example by hand) to
# enable/configure the gateway. See docs/14-model-gateway.md.

MODEL_GATEWAY_CONFIG=config/model-gateway.yaml
MODEL_GATEWAY_ENABLED=false

LMSTUDIO_BASE_URL=http://localhost:1234/v1
LMSTUDIO_MODEL=

LLAMACPP_BASE_URL=http://localhost:8080/v1
LLAMACPP_MODEL=

VLLM_BASE_URL=http://localhost:8000/v1
VLLM_MODEL=

SGLANG_BASE_URL=http://localhost:30000/v1
SGLANG_MODEL=

LITELLM_BASE_URL=http://localhost:4000/v1
LITELLM_API_KEY=
LITELLM_MODEL=
