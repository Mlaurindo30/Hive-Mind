# Canonical local-min runtime contract. Copy only non-secret defaults into .env.
VECTOR_BACKEND=sqlite_vec
MODEL_GATEWAY_MODE=on
HIVE_FORCE_LEGACY_LLM=false
HIVE_KNOWLEDGE_HEALTH_MILVUS=0
HIVE_MIND_PROFILE=local-min
