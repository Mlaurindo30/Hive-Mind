services:
  falkordb:
    image: falkordb/falkordb:latest
    container_name: sinapse-falkordb
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - falkordb_data:/data

volumes:
  falkordb_data:
