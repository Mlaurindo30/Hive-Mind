"""Native agent detection, MCP registration and capture install (hive_mind.agents).

The single canonical namespace (ADR-013) for provider detection, MCP
registration, instruction install and capture install. Replaces the logic that
lived in register-mcp.{ps1,sh}; those become thin wrappers (ADR-003).
"""
