"""Maintenance workflows (backup, retention, audit) — native package.

The verified backup engine lives in :mod:`hive_mind.maintenance.backup`.
`scripts/health/backup_databases.py` re-exports from here and keeps no copy.
"""
